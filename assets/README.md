# Assets

Offizielles SwiPay-Logo hier ablegen, damit der App-Header es einbindet:

- `swipay-logo.svg` (bevorzugt) oder `logo.svg`
- alternativ `swipay-logo.png` / `logo.png`

Ohne Datei zeigt der Header eine CI-getreue Hexagon-Wortmarke als Fallback
(siehe `src/ui.py`, `sidebar_brand()`).
