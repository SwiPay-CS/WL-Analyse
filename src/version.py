"""Single source of the tool version.

Format per CLAUDE.md: Major.Minor.Patch[.Hotfix]. Nothing read this before --
case files need it, so a file saved today still says what produced it.
"""

TOOL_VERSION = "1.0.0"
